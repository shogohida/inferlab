# System Design面接 想定問答リファレンス（inferlab深掘り編）

このドキュメントは`inferlab`の実装を題材にした、面接官が深掘りしてきそうな
質問と模範回答をまとめたものです（`internal/model/`, `internal/cache/`,
`internal/quant/`, `internal/batch/`が対象）。各回答は30〜60秒で口頭
説明できる分量です。

各項目の構成：**深掘り質問** → **模範回答** → **さらに追い質問が来たら** →
**コード参照**。

---

## 1. KVキャッシュはなぜ毎ステップO(n)の再計算をO(1)の新規計算に変えられるのか

### 深掘り質問
「そもそもなぜKVキャッシュが必要なのですか？成長していくシーケンス全体に
毎回モデルを新規に走らせるのではダメなのですか？」

### 模範回答
位置`t`での自己アテンションは、位置`0..t`すべてのキー・バリューベクトルを
必要とします。これらのキー・バリューベクトルは「その位置のトークンとモデル
の重みだけで決まる決定的な関数」であり、一度計算されれば変化しません。
キャッシュがなければ、トークン`t+1`を生成するために位置`0..t`のK/Vを
また一からすべて再計算することになります——`internal/model.ForwardSequence`
はまさにこれを行っており、毎回新しいキャッシュバッファを作って位置0から
`Step`をやり直します。長さ`n`の生成全体では、これは`1+2+...+n = O(n²)`
のStep相当の計算量になります。永続的な`cache.KVCache`
（`internal/cache/cache.go`）は、すでに計算済みのK/Vベクトルを単に保持
しておくだけです。新しいトークンごとに`Step`をちょうど1回呼び、自分の
位置のK/Vだけを書き込み、既存の`0..t-1`のエントリを読むだけなので、同じ
`n`トークンの生成が`O(n)`の総計算量で済みます。

### さらに追い質問が来たら
「キャッシュを使うとアテンションの計算結果自体が変わってしまうのでは？」
——いいえ、そしてこれこそが本当に重要な性質です。
`internal/cache/cache_test.go`の`TestCachedDecodeMatchesUncachedRecompute`
は、キャッシュを使った経路と完全な再計算経路が、毎ステップ*ビット単位で
完全に同一の*logitsを生成することを検証しています。キャッシュは純粋な
性能最適化であり、結果を変えてしまうならそれは機能ではなくバグです。

### コード参照
`internal/model/model.go`の`Step`（キャッシュの読み書き）、`internal/cache/cache.go`の`Reserve`、`internal/cache/cache_test.go`の`TestCachedDecodeMatchesUncachedRecompute`。

---

## 2. アテンションはなぜFFN層と同じようにシーケンス間でバッチ化できないのか

### 深掘り質問
「線形層はバッチ化したのにアテンションはしていませんね。それは単に楽をした
だけではなく、なぜ必要な区別なのですか？」

### 模範回答
線形射影（QKVO、FFNのgate/up/down）は「位置に依存しない」演算です：
`out[i] = sum_j W[i][j] * x[j]`は1シーケンスの現在トークンの活性化にのみ
依存し、他のどのシーケンスにも依存しません。N個のシーケンスの活性化を
1つの行列に積み重ねて`Linear.BatchMatMul`を1回呼ぶのは、N回別々に呼ぶのと
完全に等価です——`internal/model/weights.go`の`denseLinear.BatchMatMul`は
文字通り`tensor.BatchMatMul`（これ自体、N個の独立した行単位の内積に
すぎません）に委譲しています。アテンションは事情が異なります。各シーケンス
は「自分自身の」KVキャッシュ履歴に対してアテンションを計算し、continuous
batchingの下では、同じバッチtickに含まれる異なるシーケンスがそれぞれ異なる
ステップ数だけ生成を続けてきている——つまりキャッシュの長さが本当に異なり
ます。全シーケンスを最長のものに合わせてパディングし、パディング部分を
マスクしない限り、シーケンス間で形状の揃った行列を作る方法はなく、本
プロジェクトはそれをあえて実装していません。そのため`StepBatch`
（`internal/model/batch_step.go`）は線形層をレイヤーごとに1回のバッチ呼び
出しでまとめ、アテンションのスコア計算・softmax・重み付き和のステップは
単純なシーケンス・ヘッドごとのループにフォールバックします。

### さらに追い質問が来たら
「実際のシステム、例えばvLLMはこの問題をどう扱っていますか？」——vLLMの
PagedAttentionはまさにこの問題への本番向けの答えです。バッチ化された線形
代数パスと、可変長のKV履歴をパディングなしで直接扱う専用のragged/paged
attentionカーネルとを分離しています。`StepBatch`の分離（位置に依存しない
ものはバッチ化し、raggedなものはループする）は、同じ発想を小規模かつ正直な
スコープで再現したものであり、偶然の一致ではありません。

### コード参照
`internal/model/batch_step.go`の`StepBatch`、`internal/model/weights.go`の`Linear`/`BatchMatMul`、`internal/model/model_test.go`の`TestStepBatchMatchesSequentialStep`。

---

## 3. 演算量が同じなのに、バッチ化がなぜスループットを改善するのか

### 深掘り質問
「`BatchMatMul`が単にN回の独立した内積で、乗算加算の総数がN回別々に呼ぶ
のと同じなら、高速化はどこから来るのですか？」

### 模範回答
演算量の削減ではなく、メモリ帯域幅から来ています。あるレイヤーの`WQ`の
ような重み行列は、内積を計算するためにRAMから読み出す必要があります。
N個のシーケンスを`MatMul`で別々に処理すると——呼び出し間でその行列が
キャッシュに残っているかどうかによりますが——同じ重み行列をN回メモリから
ストリーミングすることになりかねません。`BatchMatMul`で処理すれば、行列は
レイヤーごとに1回読み出され、ホットな間にN個すべてのシーケンスの内積で
再利用されます。本プロジェクト自身のベンチマークパネルがこれを直接計測して
います。`internal/api/handlers.go`の`runSequential`（N個の独立した
キャッシュ付き生成を連続実行）と`runBatched`（同じN個のシーケンスを
`batch.Scheduler`で1tickずつまとめて処理）は「合計」のtokens/secを報告し、
実ハードウェア上でバッチ化が実測で優位に立ちます——`README.md`の
テスト実行／ライブデモの数値を参照してください。

### さらに追い質問が来たら
「これは常に有利なのですか？」——いいえ。バッチサイズが非常に小さい場合や
シーケンスごとの隠れ次元が非常に大きい場合、重み行列はバッチ化の有無に
関わらずすでにキャッシュに乗っている可能性があり、恩恵は小さくなります。
本プロジェクト自身の量子化の結果（下記Q6）は、まさに同じ教訓のより鋭い
バージョンです。メモリ帯域幅の議論は、ワークロードが実際に帯域幅律速で
ある場合にのみ報われます。

### コード参照
`internal/api/handlers.go`の`runSequential`/`runBatched`、`internal/batch/scheduler.go`の`Tick`。

---

## 4. continuous batchingの「continuous」とは何を意味し、admit/evictがなぜ難しいのか

### 深掘り質問
「continuous batchingと、単にN件のリクエストをまとめてバッチ化するのとの
実際の違いは何ですか？」

### 模範回答
静的バッチ化は、バッチ内の全シーケンスが終わるまで新しい仕事を受け付け
ません——N件のバッチは最も遅い（長い）メンバーの速度で進み、短いシーケンス
が早く終わってもGPU/CPUは部分的にアイドルになります。continuous batching
は代わりに、あるシーケンスが終わったスロットを、他の進行中シーケンスを
一切乱すことなく、次のtickで新しくキューに入ったシーケンスに引き渡します。
この正しさの基準は「シーケンスAを退出させ、その空いたスロットにCを受け
入れても、シーケンスBの結果が一切変わらないこと」です。
`batch.Scheduler.Evict`（`internal/batch/scheduler.go`）はマップから
エントリを削除するだけです——生き残っているシーケンスの`*cache.KVCache`
オブジェクトには一切触れないため、壊れうる共有可変状態がそもそも存在しま
せん。`TestEvictAdmitMidStreamDoesNotDisturbOthers`はこれを直接証明して
います。1つのシーケンスを退出させ、空いたスロットに新しいシーケンスを
受け入れ、生き残ったシーケンスの出力が完全に単独で実行した場合と一致する
ことを確認しています。

### さらに追い質問が来たら
「素朴な実装だと何が壊れますか？」——マップの代わりに固定インデックスの
スライスを使い、退出時に要素をシフトする実装だと、どの物理キャッシュ
バッファがどの論理シーケンスに属しているかの対応関係を静かに壊してしまい
ます——まさにこのテストがなければ検出されずに出荷されてしまいそうな
正しさのバグです。

### コード参照
`internal/batch/scheduler.go`の`Admit`/`Evict`/`Tick`、`internal/batch/scheduler_test.go`の`TestEvictAdmitMidStreamDoesNotDisturbOthers`。

---

## 5. なぜ行列全体に1つのスケールではなく、行単位の量子化なのか

### 深掘り質問
「重み行列全体に1つのスケールを使うのではなく、各行に独自のスケールで
量子化していますね。それはなぜ重要なのですか？」

### 模範回答
対称int8量子化は、`scale = max(|row|) / 127`を使って各行の値を
`[-127, 127]`にマッピングします。もしある行だけ行列の他の部分よりはるかに
大きな重みを含んでいた場合、行列全体で1つのスケールを使うとその外れ値に
支配されてしまい、他のすべての行の精度がほぼゼロまで潰れてしまいます。
スケールを各行に限定すれば、ある行の外れ値はその行にしか影響しません。
`internal/tensor/quant_test.go`の
`TestQuantizePerRowBeatsPerTensorOnOutlierRow`はこれを具体的に示して
います。2行の行列で、行0には巨大な外れ値、行1には小さく均一な値が入って
いる場合、行単位の量子化は行1の精度を保ちますが、行列全体で共有される
スケールではそれができません。

### さらに追い質問が来たら
「GGUFのグループサイズ64方式のような、さらに進んだブロック単位の量子化を
使わないのはなぜですか？」——ブロック単位の量子化はスケールの範囲をさらに
狭くします（行全体ではなく行内のNブロックごと）。同じビット幅でより高い
精度を回復できますが、行ごとではなくブロックごとのスケールという管理
コストがかかります。これは本プロジェクトの意図的なスコープの切り出しで
あり（`README.md`の既知の制限に記載）、行単位の量子化のほうが検証しやすい
正しさの範囲でありながら、最も単純な選択肢（行列全体単位）よりも十分に
正当化できる設計判断だからという理由で選びました。

### コード参照
`internal/tensor/quant.go`の`Quantize`/`QuantMatMul`、`internal/tensor/quant_test.go`の`TestQuantizePerRowBeatsPerTensorOnOutlierRow`。

---

## 6. なぜ量子化はメモリを削減したのに、本プロジェクト自身のベンチマークでは速くならなかったのか

### 深掘り質問
「READMEには、あなたのハードウェアではint8量子化がfp32より実際に*遅かった*
と書かれていますね。量子化は速くなるためのものではないのですか？」

### 模範回答
量子化による高速化は、実現する場合、メモリを流れるデータ量を減らすこと
（重み1つあたり4バイトではなく1バイト）から来ます。しかし`QuantMatMul`
（`internal/tensor/quant.go`）は、要素ごとにint8値をソフトウェアで
`float32`に変換し直し、その行のスケールを掛ける処理を依然として行って
います。int8による実ハードウェアでの高速化は、int8の乗算加算をネイティブ
に行う専用のSIMD内積命令（x86のAVX-VNNI、ARM NEONのドット積拡張命令）から
来るもので、要素ごとの浮動小数点変換を伴いません。本プロジェクトのGo実装
にはそうした命令パスがなく、移植性のあるスカラーループです。そのため
量子化された乗算加算1回のコストはfp32版より*高く*なり、メモリ帯域幅の
節約は、そのワークロードが実際に帯域幅律速である場合にのみ元が取れます。
ベンチマーク実行時にほぼCPUのキャッシュに収まってしまう約60MBのモデルは、
そもそも帯域幅律速ではない可能性があり、そのため計算オーバーヘッドが
上回り、量子化のほうが遅くなる、という結果が——推測ではなく実測で——出て
います。ここで量子化が実際にもたらす恩恵は静的なメモリ使用量です
（`internal/quant.Weights.ByteSize()`で実測した`約15MBのint8`対
`約60MBのfp32`）。これはRender無料枠の512MBというRAM予算に収めるうえで
直接効いてくる話であり、「量子化は常に速い」よりも狭いが正直な主張です。

### さらに追い質問が来たら
「このハードウェアで実際にint8の高速化を得るにはどうしますか？」——本物の
SIMD int8 GEMMカーネルを呼び出す（ただしこれは本プロジェクトの意図的な
制約である「純粋Go、MLライブラリ不使用」から外れることになります）か、
十分な数のシーケンスをまとめてバッチ化し、要素ごとの変換コストという固定
費用を、より大きなバッチ全体での帯域幅節約の合計が上回るようにする、
という方法があります——これは`internal/batch`のスケジューラがまさに実験
可能にしていることです。

### コード参照
`internal/tensor/quant.go`の`QuantMatMul`、`internal/quant/quant.go`の`ByteSize`、`README.md`記載のベンチマーク数値。

---

## 7. 因果マスキングのテストは、なぜ「出力がそれらしく見えるか」の確認ではなくこの形なのか

### 深掘り質問
「アテンションが因果境界を実際に守っていることを、生成されたテキストを
目視するのではなく、どうやって検証していますか？」

### 模範回答
アテンションループの上限に潜む微妙なオフバイワン（`t <= pos`とすべき
ところを、例えば`t < len(cache)`にしてしまうなど）があると、あるトークン
が未来の位置のキー・バリューを「見て」しまいます。バグの内容によっては、
出力は依然としてそれらしいテキストに見えつつ、目視では気づきにくい形で
微妙に間違っている可能性があります。`TestCausalMaskingIgnoresFutureCache`
（`internal/model/model_test.go`）はこれを直接観測可能にしています。検証
対象の位置より*先*にあるすべてのキャッシュスロットを、それらが正当に
書き込まれるより前に極端な値（`1e6`）で埋めておき、対象位置のlogitsを
計算して、それらの未来スロットに一切触れなかった場合の実行結果と同一で
あることを検証します。もしアテンションが因果境界を越えて読んでしまって
いれば、仕込んだ極端な値がsoftmaxを目に見えて破綻させ、出力を変えて
しまうはずです——このテストは「少し悪化したテキスト」を静かに生むのでは
なく、はっきりと失敗します。

### コード参照
`internal/model/model.go`の`Step`（`for t := 0; t <= pos; t++`のアテンション
ループ）、`internal/model/model_test.go`の`TestCausalMaskingIgnoresFutureCache`。
